# web/

Deferred. Priority 3 in `docs/CODE_STANDARD.md`'s component table — not yet built.

When this starts: it depends on `protocol/` like every other client, and gets its own
`AGENTS.md` and CI job at that point. Nothing here yet is a decision, just a reserved slot in
the repo layout ([A1](../docs/CODE_STANDARD.md#standard-a1)).
